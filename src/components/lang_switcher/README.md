# Language Switcher

## Image

![langSwitcher-component](/doc/langSwitcher-component.png)


## Description

The `Language Switcher` component displays the available locales as interactive buttons. When a user selects a language, it updates the active locale and stores the choice in local storage so the preference persists across reloads.
